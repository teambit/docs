import React from 'react';
import { Theme } from './theme';

export const BasicTheme = () => (
  <Theme>
    <div style={{ color: 'primary-color' }}>
      Anything can be rendered inside the theme
    </div>
  </Theme>
);
